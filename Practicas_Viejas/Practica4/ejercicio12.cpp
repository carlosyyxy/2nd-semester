/*Realice un algoritmo que permita colorear un mapamundi (en caso de ser posible). La
manera de colorear el mapa es la siguiente, para que un color sea válido para un determinado
país debe cumplirse que sea distinto del que tienen asignado todos sus países limítrofes. Se
debe utilizar la menor cantidad de colores posibles. Asuma que existen n países distintos, y
que se cuenta con una matriz de dimensiones n x n de tipo bool, que indica si un país es
limítrofe de otros. Por último, se debe considerar que los colores se representan mediante
enteros*/

#include<iostream>
using namespace std;
int sumavalores(int x);

int main(){

  return 0;
}
